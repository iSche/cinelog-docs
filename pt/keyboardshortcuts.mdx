---
title: Atalhos de teclado
lastUpdated: 2026-09-04
description: "Todos os atalhos de teclado do CineLog, agrupados por contexto. O diálogo dentro do app (Cmd / Ctrl + K) é uma referência separada, mantida à mão, que pode ficar desatualizada."
---

Usuários de Mac veem `Cmd`; usuários de Windows substituem por `Ctrl`. O diálogo no app aberto com `Cmd / Ctrl + K` é uma referência parecida, mantida separadamente à mão, então pode ficar desatualizada em relação a esta página.

O celular não exibe uma referência de atalhos — não há diálogo no app no celular, já que o uso em celular normalmente não envolve atalhos de teclado. Usuários de tablet com teclado físico podem usar todos os atalhos listados aqui.

## Global

- `[` — alterna a barra lateral esquerda (expandida ↔ recolhida)
- `Cmd / Ctrl + K` — abre o diálogo de atalhos de teclado
- `Cmd / Ctrl + Shift + M` — alterna o tema (claro / escuro)

## Decupagem (Linear e Produção)

- `Cmd / Ctrl + N` — adiciona um plano (ou um banner se estiver na visualização Produção)
- `Shift + D` — alterna as [Configurações de Exibição](/pt/guides/shot-list/display-settings)
- `Shift + L` — alterna o layout de colunas (ajustado ↔ rolagem)
- `Shift + M` — alterna entre altura de linha compacta ↔ expandida
- `Shift + S` — alterna o painel lateral do roteiro. Somente na visualização Linear, na Produção não faz nada
- `1` a `9` — alterna a visibilidade das primeiras nove colunas após as colunas fixas Status / Plano / Dia
- `Esc` — limpa a seleção atual
- `Delete` / `Backspace` — exclui em lote os itens selecionados

## Editor de Roteiro

- `Cmd / Ctrl + 1` a `7` — define o tipo do parágrafo atual (1 cabeçalho de cena, 2 personagem, 3 diálogo, 4 ação, 5 parêntese, 6 transição, 7 letra de música)
- `Cmd / Ctrl + B` / `I` / `U` — negrito / itálico / sublinhado
- `Cmd / Ctrl + E` — define o parágrafo atual como o tipo de elemento texto-centralizado (não um alternador geral de alinhamento)
- `Cmd / Ctrl + I` — com o cursor sobre um texto, alterna o itálico. Caso contrário, inicia uma importação de roteiro (o seletor de arquivos e a [revisão de importação](/pt/guides/importing-scripts)) — o mesmo que o botão **Import**, que carrega o atalho no próprio rótulo
- `Shift + Return` — quebra de linha dentro do elemento atual
- `Shift + P` — Popular a partir do Roteiro (roda aqui no Editor de Roteiro; inerte na decupagem)

### Zoom

- `Cmd / Ctrl + =` / `Cmd / Ctrl + -` — aproxima / afasta o zoom (passos de 25%, 50–200%)
- `Cmd / Ctrl + 0` — redefine o zoom para ajustar à largura
- `Cmd / Ctrl + scroll` — zoom no ponteiro (pinça também funciona)

### Página de título

- `Tab` / `Shift + Tab` — próximo campo / campo anterior da página de título, com o valor pré-selecionado; `Tab` no último campo entra no corpo do roteiro

## Editor de marcações da Análise Técnica

- `1` a `9` — escolhe uma das primeiras nove categorias (enquanto nenhum campo estiver em foco)
- `Tab` — pula para o campo Elemento com o texto selecionado
- `Enter` — salva a marcação
- `Esc` — fecha sem salvar

## Exportações

- `Cmd / Ctrl + P` — exporta a visualização atual para PDF na decupagem e no storyboard; no Roteiro AV, abre o diálogo de compartilhamento em modo de exportação. Não está vinculado nas ordens do dia — use o botão Exportar na barra de menu superior da ordem do dia.

## Notas

- `Cmd / Ctrl + N` é sensível ao contexto: um plano na visualização Linear, um banner na Produção. `Shift + S` só funciona na Linear. A lista acima descreve os dois.
- O diálogo no app (`Cmd / Ctrl + K`) mostra a maioria desses atalhos, mas não todos — ele não tem uma seção de Roteiro AV, e sua lista é mantida à mão, então pode ficar desatualizada em relação a esta página.
